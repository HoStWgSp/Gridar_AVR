/*
* ƒанный макрос используетс€ дл€ вывода на дисплей символа
* из таблицы ASCII, заключенный в одиночные апострофы
*/
.macro HD44780U_Write_Symbol

	push r16					; вталкиваем r16 в STACK

	ldi r16, @0					; записываем @0 в r16
	HD44780U_Send_data r16		; отсылаем данные
	HD44780U_DDRAM_Increment	; провер€ем на окончание строки

	pop r16						; выталкиваем r16 из STACK

.endm
