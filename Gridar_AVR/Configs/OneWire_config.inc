One_Wire_Pin 'C', 0				; буква порта, номер ножки порта
One_Wire_MOSFET_Pin 'B', 2		; буква порта, номер ножки порта

#define ExternalPowerSupply		; Если используете паразитное питание, то закоментируйте строку

